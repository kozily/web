version: '2'
services:
  npm:
    image: kozily/web
    build: .
    entrypoint: npm
    command: start
    volumes:
      - "./app:/opt/project/app"
      - "./config:/opt/project/config"
      - "./package.json:/opt/project/package.json"
      - "./specs:/opt/project/specs"
    ports:
      - "8080:8080"
